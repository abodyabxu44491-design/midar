-- =====================================================================
-- مِدار | MIDAR — الترحيل 0034: صفحة المدرسة العامة كموقع مصغّر
--   about: نبذة عن المدرسة (تكتبها الإدارة)
--   show_contact: إظهار بيانات التواصل (من ملف المدرسة) في الصفحة العامة
-- كل عنصر يبقى اختياريًا وتتحكم به الإدارة، ولا تُعرض أي بيانات طالب غير مصرح بها.
-- =====================================================================
ALTER TABLE school_public_settings
  ADD COLUMN about        text CHECK (char_length(about) <= 800),
  ADD COLUMN show_contact boolean NOT NULL DEFAULT true;
-- إيجاد الطالب بمعرّفه في الصفحة العامة: فهرس فريد موجود أصلًا (tenant_id, access_key)
